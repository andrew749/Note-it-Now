class WordImage:
    """
    Holds the rect which represents a word, as well as the actual word stored in that rect
    Data structure to use for indexing.
    """
    def __init__(self, rect, text):
        self.rect = rect
        self.text = text

